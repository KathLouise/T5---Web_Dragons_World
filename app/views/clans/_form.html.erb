<%= form_for(@clan) do |f| %>
  <% if @clan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@clan.errors.count, "error") %> prohibited this clan from being saved:</h2>

      <ul>
      <% @clan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :dragon_id %><br>
    <%= f.collection_select :dragon_id, Dragon.order(:name),:id,:name, include_blank: true %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :species %><br>
    <%= f.text_field :species %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
